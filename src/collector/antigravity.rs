// Antigravity-specific logic (if any).
// Currently all collection is handled by the shared dispatch log parser in mod.rs.
// This module is reserved for future Antigravity-specific features:
// - Reading conversation SQLite DBs for per-turn token data
// - Google quota API integration (deferred)
